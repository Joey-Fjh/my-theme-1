name = "verifier"
description = "Independent read-only reviewer for implementation, architecture, and completion claims."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read and follow .agents/roles/verifier.md before acting. Rebuild context from repository evidence and the actual diff. Do not edit or inherit implementation conclusions as facts, never create or delegate to another agent, and return a result that maps to .agents/contracts/result.schema.json.
"""
