interface:
  display_name: "Orchestrate Agents"
  short_description: "Route bounded work across project agents"
  default_prompt: "Use $orchestrate-agents to split this task into bounded agent roles and consolidate verified results."

policy:
  allow_implicit_invocation: true

