<div
    data-section-id="{{ section.id }}"
    class="
        main-page-about-section full-width relative overflow-hidden color-{{ section.settings.color_scheme }}
        min-h-168 pc:min-h-0
    "
    style="
        --section-padding-top: {{ section.settings.padding_top }}px;
        --section-padding-bottom: {{ section.settings.padding_bottom }}px;
    "
    data-component-kind="section"
    data-component-type="main-page-about"
    data-component-id="{{ section.id }}"
>
    <div
        data-gsap-image
        class="absolute inset-0 h-full w-full pc:static pc:h-auto"
    >
        {%- capture sizes -%}
            (min-width: 80rem) 100vw, (min-width: 48rem) 1280px, 768px
        {%- endcapture -%}

        {%- capture widths -%}
            400,768,1280,1536,2560
        {%- endcapture -%}

        {%-
            render 'image',
            image: section.settings.image,
            alt: section.settings.image_alt | default: 'About Us' | escape,
            width: section.settings.image.width,
            sizes: sizes,
            widths: widths,
            class: 'h-full w-full pc:h-auto',
            img_class: 'h-full w-full object-cover object-center'
        -%}
    </div>

    <div class="layout container-page absolute inset-0">
        <div class="mx-auto flex h-full w-full max-w-lg flex-col items-center justify-start gap-3 px-6 pt-28 text-center text-white pc:max-w-1/2 pc:justify-center pc:gap-5 pc:pt-0">
            <h3
                data-gsap-item
                class="h5 text-theme-text pc:h3"
            >
                About Eccenova
            </h3>
            <h1
                data-gsap-item
                class="h2 font-bold pc:hxl"
            >
                A Gentle Approach to Better Skin
            </h1>
            <h3
                data-gsap-item
                class="body-sm opacity-80 pc:body-md"
            >
                Because your skin deserves more than promises.
            </h3>
        </div>
    </div>
</div>

{%- javascript -%}
    (function () {
        const Components = window.__Theme__?.Components;
        if (!Components || typeof gsap === 'undefined') return;

        Components.register(
            'main-page-about',
            {
                init(el) {
                    const ctx = gsap.context(() => {
                        const imageWrapper = el.querySelector('[data-gsap-image]');
                        const image = imageWrapper?.querySelector('img');
                        const items = el.querySelectorAll('[data-gsap-item]');

                        if (!image || !items.length) return;

                        gsap.set(image, { scale: 1.1, opacity: 0 });
                        gsap.set(items, { y: 40, opacity: 0 });

                        const tl = gsap.timeline({
                            defaults: { ease: 'power2.out' },
                            scrollTrigger: {
                                trigger: el,
                                start: 'top 80%',
                                once: true,
                            },
                        });

                        tl.to(image, {
                            scale: 1,
                            opacity: 1,
                            duration: 1.3,
                        });

                        tl.to(
                            items,
                            {
                                y: 0,
                                opacity: 1,
                                duration: 0.9,
                                stagger: 0.2,
                            },
                            '-=0.8',
                        );
                    }, el);

                    return { ctx };
                },

                destroy(el, state) {
                    if (state?.ctx) {
                        state.ctx.revert();
                    }
                },
            },
            { lazy: true },
        );
    })();
{%- endjavascript -%}

{%- stylesheet -%}
{%- endstylesheet -%}

{% schema %}
{
    "name": "About Us",
    "tag": "section",
    "class": "section",
    "enabled_on": {
        "templates": ["page"]
    },
    "settings": [
        {
            "type": "color_scheme",
            "id": "color_scheme",
            "label": "Color scheme",
            "default": "scheme-1"
        },
        {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
        },
        {
            "type": "text",
            "id": "image_alt",
            "default": "About Us",
            "label": "Image alt text"
        },
        {
            "type": "header",
            "content": "Padding"
        },
        {
            "type": "range",
            "id": "padding_top",
            "min": 0,
            "max": 100,
            "step": 4,
            "unit": "px",
            "label": "Padding Top",
            "default": 0
        },
        {
            "type": "range",
            "id": "padding_bottom",
            "min": 0,
            "max": 100,
            "step": 4,
            "unit": "px",
            "label": "Padding Bottom",
            "default": 0
        }
    ],
    "presets": [
        {
            "name": "About Us",
            "settings": {}
        }
    ]
}
{% endschema %}
