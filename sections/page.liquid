{% comment %}
    This section is used in the page template to render store pages like About us
    or Contact us.
{% endcomment %}
<div
    data-section-id="{{ section.id }}"
    class="page-section full-width color-{{ section.settings.color_scheme }}"
    style="
        --section-padding-top: {{ section.settings.padding_top }}px;
        --section-padding-bottom: {{ section.settings.padding_bottom }}px;
    "
>
    <div class="layout container-page bg-theme-bg text-theme-text">
        <div class="mx-auto flex w-full max-w-2/3 flex-col gap-8">
            <h1>{{ page.title | escape }}</h1>
            <div class="rte w-full">
                {{ page.content }}
            </div>
        </div>
    </div>
</div>

{% schema %}
{
    "name": "t:general.page",
    "tag": "section",
    "class": "section",
    "settings": [
        {
            "type": "color_scheme",
            "id": "color_scheme",
            "label": "t:sections.page.settings.color_scheme",
            "default": "scheme-2"
        },
        {
            "type": "header",
            "content": "t:sections.page.content.padding"
        },
        {
            "type": "range",
            "id": "padding_top",
            "min": 0,
            "max": 100,
            "step": 4,
            "unit": "px",
            "label": "t:sections.page.settings.padding_top",
            "default": 0
        },
        {
            "type": "range",
            "id": "padding_bottom",
            "min": 0,
            "max": 100,
            "step": 4,
            "unit": "px",
            "label": "t:sections.page.settings.padding_bottom",
            "default": 0
        }
    ]
}
{% endschema %}
