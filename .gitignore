# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
node_modules/*

# Shopify CLI files
.shopify/*

## Release files
release
*.zip

# Icons (generated / processed)
icons/*

# Claude Code local settings (contains API keys)
.claude/settings.local.json

# Local secrets (never commit)
.env
.env.*
!.env.example

# Playwright MCP local session artifacts
.playwright-mcp/

